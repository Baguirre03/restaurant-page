import { createMainContainer, createChildren, createButton } from "./basicFunctions";

//For functions (text, parentClass, className)
const renderHomePage = () => {
    createMainContainer('textawadw', 'class', 'class');
}

export {renderHomePage}